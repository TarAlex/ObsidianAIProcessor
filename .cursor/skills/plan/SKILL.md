# Plan

Design a new module. Saves spec before any code is written.
Usage: `/plan`

---

Use the dev-planner workflow: @dev-planner

Tell it: "The user wants to plan a new module for obsidian-agent.
Read `.cursor/dev/TRACKER.md` and `docs/ARCHITECTURE.md` first,
then interview the user with AskQuestion before writing any spec
to `.cursor/dev/specs/SLUG.md`."
