# Review

Review a module before marking it done.
Usage: `/review agent/path/to/module.py`

---

Use the dev-reviewer workflow: @dev-reviewer

Tell it: "Review [MODULE_PATH] against the full checklist.
Output APPROVED or NEEDS_CHANGES with line references."

Replace `[MODULE_PATH]` with the path provided by the user.
